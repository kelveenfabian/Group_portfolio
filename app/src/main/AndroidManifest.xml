<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pursuit.Group_portfolio_HW_TEAM_THE_TRIPLE_THREAT">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".veenPortfolio"
            android:parentActivityName=".ChoosePerson">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".JohnActivity"
            android:parentActivityName=".ChoosePerson" />
        <activity android:name=".ChoosePerson" />
        <activity android:name=".EvelynActivity"></activity>
    </application>

</manifest>